import styled from '@emotion/styled';

export const Input = styled.input`
  display: inline-block;
  width: 100%;
  font: inherit;
  font-size: 16px;
  padding: 4px;

  &::placeholder {
    font: inherit;
    font-size: 16px;
  }
`;
